# docker-compose.yml
version: '3'
services:
  api:
    build: .
    ports:
      - 3000:3000
    depends_on:
      - mongodb
      # - rabbitmq
      - redis
  mongodb:
    image: mongo
    ports:
      - 27017:27017
  # rabbitmq:
  #   image: rabbitmq:3-management
  #   ports:
  #     - 5672:5672
  #     - 15672:15672
  redis:
    image: redis
    ports:
      - 6379:6379